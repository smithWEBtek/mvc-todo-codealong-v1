
<!-- <li class="<%= "completed" if item.complete?  %>"> -->
<!-- <li class=<%= li_class_for_item(item) %>> -->
<!--content_tag_for   -->
<!--                CSS element -->
<!--                      using 'item' local variable-->
<!--                            define a class using helper method -->
<!--                                                          do --> 
<%= content_tag_for :li, item, class: li_class_for_item(item) do %>
  <div class="view" >
      <%= form_for([@list, item]) do |f| %>
        <%= f.check_box :status, :class=>"toggle", :checked=> (true if item.complete?) %>
      <% end %>
      <label><%= item.description %></label>
   </div>
</li>
<% end %>
